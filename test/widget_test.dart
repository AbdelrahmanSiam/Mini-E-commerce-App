// Smoke test placeholder — full app tests need Hive + GetIt setup (see main.dart).
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('sanity', () {
    expect(1 + 1, 2);
  });
}
